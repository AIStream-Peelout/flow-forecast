version: 2.1

orbs:
  codecov: codecov/codecov@1.0.2

executors:
  python-executor:
    docker:
      - image: cimg/python:3.11
    working_directory: ~/repo


defaults: &defaults
  executor: python-executor
  working_directory: ~/repo

jobs:
  setup_and_install:
    <<: *defaults
    steps:
      - checkout
      - persist_to_workspace:
          root: .
          paths:
            - .
  linting:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo

      - restore_cache:  # ensure this step occurs *before* installing dependencies
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}

      - run:
          name: install dependencies
          command: |
            python --version
            pip install flake8
            python -m flake8 .

  evaluator_tests:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo

      - restore_cache:  # ensure this step occrurs *before* installing dependencies
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}

      - run:
          name: install dependencies
          command: |
             pip install pipenv
             pip install coverage
             pip install --quiet --force-reinstall  --upgrade -r requirements.txt
             python setup.py develop --no-deps

      - save_cache:
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}
          paths:
            - ~/.local

      - run:
          name: Evaluator tests
          when: always
          command: |
            coverage run -m unittest -v tests/test_deployment.py
            coverage run -m unittest -v tests/test_evaluation.py
            coverage run -m unittest -v tests/validation_loop_test.py
            coverage run -m unittest -v tests/test_handle_multi_crit.py

      - run:
          name: upload
          when: always
          command: bash <(curl -s https://codecov.io/bash) -cF python

      - store_test_results:
          path: tests-results

      - store_artifacts:
          path: tests-results
          destination: tests-results-evaluator


  data_quality_tests:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - restore_cache:  # ensure this step occurs *before* installing dependencies
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}

      - run:
          name: install dependencies
          command: |
             pip install pipenv
             pip install coverage
             pip install --force-reinstall --upgrade -r requirements.txt
             python setup.py develop --no-deps

      - save_cache:
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}
          paths:
            - ~/.local

      - run:
          name: Data Quality tests
          command: |
            coverage run -m unittest -v tests/test_multitask_decoder.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/data_format_tests.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/usgs_tests.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_preprocessing.py
            coverage run -m unittest -v tests/test_variable_length.py

      - run:
          name: upload_stuff
          when: always
          command: bash <(curl -s https://codecov.io/bash) -cF python

      - store_test_results:
          path: tests-results

      - store_artifacts:
          path: tests-results
          destination: tests-results-data-quality

  model_basic_tests:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - restore_cache:  # ensure this step occurs *before* installing dependencies
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}

      - run:
          name: install dependencies
          command: |
             pip install pipenv
             pip install coverage
             pip install --force-reinstall --upgrade --quiet -r requirements.txt
             python setup.py develop --no-deps

      - save_cache:
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}
          paths:
            - ~/.local

      - run:
          name: Model basic tests
          when: always
          command: |
            coverage run -m unittest -v tests/multi_modal_tests/test_cross_vivit.py
            coverage run -m unittest -v tests/test_series_id.py
            coverage run -m unittest -v tests/test_squashed.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_attn.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_informer.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_json_config/tests_meta_pr.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_json_config/time_model_tests.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_json_config/data_loader_tests.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_json_config/tests_da_rnn.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_json_config/tests_loss.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_json_config/tests_merging_models.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_json_config/pytorc_train_tests.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/tests_classification2_loader.py
      - run:
          name: upload_stuff
          when: always
          command: bash <(curl -s https://codecov.io/bash) -cF python

      - store_test_results:
          path: tests-results

      - store_artifacts:
          path: tests-results
          destination: tests-results-model-tests

  decoder_tests:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - restore_cache:  # ensure this step occurs *before* installing dependencies
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}

      - run:
          name: install dependencies
          command: |
             pip install pipenv
             pip install coverage
             pip install --upgrade --force-reinstall --quiet -r requirements.txt
             python setup.py develop --no-deps

      - save_cache:
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}
          paths:
            - ~/.local

      - run:
          name: Decoder tests
          when: always
          command: coverage run -m unittest-v tests/test_decoder.py

      - run:
          name: upload_stuff
          when: always
          command: bash <(curl -s https://codecov.io/bash) -cF python

      - store_test_results:
          path: tests-results

      - store_artifacts:
          path: tests-results
          destination: tests-results-decoder


  trainer_tests:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - restore_cache:  # ensure this step occurs *before* installing dependencies
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}

      - run:
          name: install dependencies
          command: |
             pip install pipenv
             pip install --force-reinstall --upgrade --quiet -r requirements.txt
             pip install coverage
             python setup.py develop --no-deps
      - save_cache:
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}
          paths:
            - ~/.local

      - run:
          name: Trainer tests
          when: always
          command: |
            echo -e 'GRU Vanilla tests'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/gru_vanilla.json
            echo -e 'Running classification tests'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/classification_test.json
            echo -e 'Running informer single tests'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/tests_inf_single.json
            echo -e 'Running transformer_gaussian tests'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/transformer_gaussian.json
            echo -e 'Running multi_decoder_tests.json'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/multi_decoder_tests.json
            echo -e 'training multi-task-decoder'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/multitask_decoder.json
            echo -e 'running da-meta data unit tests'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/da_meta.json
            echo -e 'running transformer bottleneck'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/transformer_bottleneck.json
            echo -e 'running da_rnn probabilistic tests'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/da_rnn_probabilistic.json
            echo -e 'running lstm probabilistic tests'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/lstm_probabilistic_tests.json
            echo -e 'running probabilistic linear reg tests'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/probabilistic_linear_regression_tests.json
            echo -e 'running meta-data fusion'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/meta_data_tests.json

      - store_test_results:
          path: tests-results

      - store_artifacts:
          path: tests-results
          destination: tests-results-trainer

      - run:
          name: upload results
          when: always
          command: bash <(curl -s https://codecov.io/bash) -cF python


  trainer_tests1:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - restore_cache:  # ensure this step occurs *before* installing dependencies
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}

      - run:
          name: install dependencies
          command: |
             pip install pipenv
             pip install --force-reinstall --upgrade --quiet -r requirements.txt
             pip install coverage
             python setup.py develop --no-deps
      - save_cache:
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}
          paths:
            - ~/.local

      - run:
          name: Trainer1 tests
          when: always
          command: |
            echo -e 'running tests_informer \n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/tests_informer.json
            echo -e 'running tests_iTransformer \n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/tests_iTransformer.json
            echo -e 'running tsmixer_tests \n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/tsmixer_tests.json
            echo -e 'running transformer_b_series.json \n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/transformer_b_series.json
            echo -e 'running crossformer.json \n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/cross_former.json
            echo -e 'running n-linear.json \n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/nlinear.json
            echo -e 'running dsanet_3\n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/dsanet_3.json
            echo -e 'running variable_autoencoderl.json\n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/variable_autoencoderl.json
            echo -e 'running dlinear\n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/dlinear.json
            echo -e 'running custom_encode.json\n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/custom_encode.json
            echo -e 'running multi_decoder_tests \n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/multi_decoder_tests.json
            echo -e 'tests_dual \n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/tests_dual.json
            echo -e 'running dsanet \n'
            coverage run flood_forecast/trainer.py -p tests/test_json_config/dsanet.json

      - store_test_results:
          path: tests-results

      - store_artifacts:
          path: tests-results
          destination: tests-results-trainer2

      - run:
          name: upload results
          when: always
          command: bash <(curl -s https://codecov.io/bash) -cF python

  trainer_tests2:
      <<: *defaults
      steps:
        - attach_workspace:
            at: ~/repo
        - restore_cache:  # ensure this step occurs *before* installing dependencies
            key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}

        - run:
            name: install dependencies
            command: |
              pip install pipenv
              pip install --force-reinstall --upgrade --quiet -r requirements.txt
              pip install coverage
              python setup.py develop --no-deps
        - save_cache:
            key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}
            paths:
              - ~/.local

        - run:
            name: Trainer tests 2
            when: always
            command: |
              echo -e 'running trainer_lstm_tests \n '
              coverage run flood_forecast/trainer.py -p tests/test_json_config/lstm_test.json
              echo -e 'running trainer_multi_tests \n'
              coverage run flood_forecast/trainer.py -p tests/test_json_config/multi_test.json
              echo -e 'running trainer_full_transformer_tests \n'
              coverage run flood_forecast/trainer.py -p  tests/test_json_config/full_transformer.json
              echo -e 'running da_rnn_tests \n'
              coverage run flood_forecast/trainer.py -p tests/test_json_config/da_rnn.json
              echo -e 'running auto_encoder \n'
              coverage run flood_forecast/meta_train.py -p tests/test_json_config/auto_encoder.json
              echo -e 'running trainer_decoder_tests   \n'
              coverage run flood_forecast/trainer.py -p tests/test_json_config/decoder_test.json
        - store_test_results:
            path: tests-results

        - store_artifacts:
            path: tests-results
            destination: tests-results-trainer

        - run:
            name: upload results
            when: always
            command: bash <(curl -s https://codecov.io/bash) -cF python

  plot_tests:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - restore_cache:  # ensure this step occurs *before* installing dependencies
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}

      - run:
          name: install dependencies
          command: |
             pip install pipenv
             pip install coverage
             pip install --upgrade --force-reinstall --quiet -r requirements.txt
             python setup.py develop --no-deps

      - save_cache:
          key: requirements-v2-{{ .Branch }}-{{ checksum "requirements.txt" }}
          paths:
            - ~/.local

      - run:
          name: plot tests
          when: always
          command: |
            coverage run -m unittest -v tests/test_plot.py
            bash <(curl -s https://codecov.io/bash) -cF python
            coverage run -m unittest -v tests/test_json_config/test_explain_model_output.py
      - run:
          name: upload_stuff
          when: always
          command: bash <(curl -s https://codecov.io/bash) -cF python

      - store_test_results:
          path: tests-results

      - store_artifacts:
          path: tests-results
          destination: tests-results-plot


workflows:
  version: 2

  tests_and_build:
    jobs:
      - setup_and_install
      - linting:
          requires:
            - setup_and_install
      - evaluator_tests:
          requires:
            - setup_and_install
      - data_quality_tests:
          requires:
            - setup_and_install
      - model_basic_tests:
          requires:
           - setup_and_install
      - decoder_tests:
          requires:
            - setup_and_install
      - trainer_tests:
          requires:
            - linting
      - plot_tests:
          requires:
            - setup_and_install
      - trainer_tests1:
          requires:
            - setup_and_install
      - trainer_tests2:
          requires:
            - setup_and_install
