build
dist
flood_forecast.egg-info
*.DS_STORE
*.pyc
tests/runs
tests/output/
.ipynb_checkpoints
.pytest_cache/
data
mypy
.mypy_cache
*.png
*.svf
*.svg
wandb
.idea/*
# Artifacts written to the repo root by training/test runs (model checkpoints,
# temp prediction CSVs, and downloaded ASOS/USGS CSVs). Real data lives in tests/ and data/.
model_save/
/*.csv

# Local environment (WANDB_API_KEY etc.) — never commit
.env

# Training run artifacts (checkpoints, eval reports, forecast plots, logs)
/output/
/checkpoint.pth
__pycache__/

# Catchment-foundation experiments: keep the scripts, ignore generated outputs.
# Manifests are regenerated by build_manifest.py and contain machine-local paths.
experiments/**/runs/
experiments/**/*.log
experiments/**/manifests/